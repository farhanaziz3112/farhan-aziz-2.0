/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    domains: ["images.unsplash.com"], // Add any other domains you are using here
  },
};

export default nextConfig;
